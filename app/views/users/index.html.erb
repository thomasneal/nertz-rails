<h1>Users</h1>


<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th>Email</th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
  </tr>
  <% end %>
</table>
<%= link_to "Add User", new_user_path, class: "btn btn-primary" %>
